#describe "I'm on the registration page", ->
#  a = false
#  it "I can see the input field for user name", ->
#    inputUserName = $ '#userName' ##Todo, get the element
#    inputEmailAddress = $ '#emailAddress' ##Todo, get the element
#    displayBlock = 'displayBlock' ##Todo, get the class
#    expect(inputUserName.className).toBe(displayBlock)
#    expect(inputEmailAddress.className).toBe(displayBlock)
#    
#    ##var $ = require('gulp-load-plugins')({lazy: true});
#
#  xit "let's me enter my user name", ->
#    a = true
#    ##test
#    expect(a).toBe(true)

describe "I'm on the Enter-the-password page", ->
  a = false
  xit "let's me enter my password", ->
    a = true
    ##test
    expect(a).toBe(true)

describe "I'm on the verification page", ->
  a = false
  xit "let's me verify my settings", ->
    a = true
    ##test
    expect(a).toBe(true)