var bleu, devisor, number;

number = 42;

devisor = 2;

bleu = 'a color';

devide(function() {
  return number / devisor;
});

multiply(function() {
  return number * devisor;
});

add(function() {
  return number + number;
});

zero(function() {
  return number - number;
});
